//Optional Material UI Theme customization if extracted

import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: { main: '#388e3c' },
    secondary: { main: '#fbc02d' },
  },
});

export default theme;
